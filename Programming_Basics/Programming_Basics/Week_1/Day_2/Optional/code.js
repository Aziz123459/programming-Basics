// this variable have 0 - what would this variable do?
var likeCount = 0;
// count the likes - what would be the function of this code block?
function increaseLikes() {
    // add +1 to the like count after anny like in crease - what is this sort operation for?
    likeCount = likeCount + 1;
}
